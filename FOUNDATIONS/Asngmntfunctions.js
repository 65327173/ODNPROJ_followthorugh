function add7(num) {

    return num + 7;
}

function multiply(num, num2) {

    
    return num * num2;
}

function capitalize(string) {

    let uc = string.substr(0, 0).toUpperCase;
    let lc = string.substr(1).toLowerCase;
    return uc.concat(lc); 
}

function lastLetter(string) {

return string.charAt(string.length - 1);
}